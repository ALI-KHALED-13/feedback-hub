import styled from "styled-components";

export const StyledTagsFilter = styled.section`
  display: flex;
  flex-wrap: wrap;
  gap: 1.4rem 0.8rem;
  padding: 2.4rem;
  background-color: ${(props) => props.theme.white};
  border-radius: 1rem;
`;
